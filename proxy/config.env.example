# Forward Proxy Configuration
# Copy this file to config.env and modify as needed

# Target AI server configuration
# Default: OpenAI API server
TARGET_HOST=api.openai.com
TARGET_PORT=443
TARGET_PROTOCOL=https

# Proxy listen configuration
# Listen on all interfaces (0.0.0.0) or specific IP
LISTEN_HOST=0.0.0.0
# Common proxy port (change if needed)
LISTEN_PORT=8080

# Caddy binary path (relative to proxy directory)
# Default: ./caddy (will be downloaded by setup.sh)
CADDY_BINARY=./caddy
