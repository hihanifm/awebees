name: Build Windows Installers

on:
  workflow_dispatch:
  release:
    types: [published, created]
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-installers:
    runs-on: windows-latest
    env:
      # Authenticate GitHub CLI (gh) when used inside Actions
      GH_TOKEN: ${{ github.token }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Read version
        id: version
        run: |
          $version = Get-Content VERSION -Raw | ForEach-Object { $_.Trim() }
          echo "version=$version" >> $env:GITHUB_OUTPUT
          echo "Version: $version"
      
      - name: Download packages (if from release)
        if: github.event_name == 'release'
        run: |
          $packageWithPython = "lens-package-with-python-${{ steps.version.outputs.version }}.zip"
          $packageRequiresPython = "lens-package-requires-python-${{ steps.version.outputs.version }}.zip"
          
          # Try to download from release assets
          $ErrorActionPreference = "Continue"
          gh release download ${{ github.event.release.tag_name }} --pattern "$packageWithPython" --pattern "$packageRequiresPython" 2>&1 | Out-Null
          if ($LASTEXITCODE -ne 0) {
            Write-Warning "Packages not found in release, will use packages from repository"
          }
      
      - name: Verify or build packages
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $packageWithPython = "dist/windows/lens-package-with-python-$version.zip"
          $packageRequiresPython = "dist/windows/lens-package-requires-python-$version.zip"
          
          $packagesFound = $true
          
          # Check if packages exist in repository
          if (-not (Test-Path $packageWithPython) -and -not (Test-Path "lens-package-with-python-$version.zip")) {
            Write-Warning "Package not found: $packageWithPython"
            $packagesFound = $false
          }
          if (-not (Test-Path $packageRequiresPython) -and -not (Test-Path "lens-package-requires-python-$version.zip")) {
            Write-Warning "Package not found: $packageRequiresPython"
            $packagesFound = $false
          }
          
          if (-not $packagesFound) {
            Write-Host ""
            Write-Host "================================================" -ForegroundColor Yellow
            Write-Host "PACKAGES NOT FOUND" -ForegroundColor Yellow
            Write-Host "================================================" -ForegroundColor Yellow
            Write-Host ""
            Write-Host "To fix this, run the following locally:" -ForegroundColor Cyan
            Write-Host "  1. ./scripts/build-windows.sh" -ForegroundColor White
            Write-Host "  2. git add dist/windows/*.zip" -ForegroundColor White
            Write-Host "  3. git commit -m 'Add Windows packages for v$version'" -ForegroundColor White
            Write-Host "  4. git push" -ForegroundColor White
            Write-Host ""
            Write-Host "Then re-run this workflow." -ForegroundColor Cyan
            Write-Host ""
            exit 1
          }
          
          echo "Packages verified successfully"
      
      - name: Install NSIS
        run: |
          # Install NSIS using Chocolatey (if available) or download directly
          $nsisPath = $null
          
          if (Get-Command choco -ErrorAction SilentlyContinue) {
            Write-Host "Installing NSIS via Chocolatey..."
            choco install nsis -y
            # Chocolatey installs to Program Files (x86)
            $nsisPath = "C:\Program Files (x86)\NSIS"
          } else {
            # Download and install NSIS manually
            $nsisUrl = "https://sourceforge.net/projects/nsis/files/NSIS%203/3.09/nsis-3.09-setup.exe/download"
            $nsisInstaller = "$env:TEMP\nsis-installer.exe"
            Write-Host "Downloading NSIS..."
            Invoke-WebRequest -Uri $nsisUrl -OutFile $nsisInstaller -UseBasicParsing
            Write-Host "Installing NSIS (silent mode)..."
            Start-Process -FilePath $nsisInstaller -ArgumentList "/S" -Wait -NoNewWindow
            $nsisPath = "C:\Program Files (x86)\NSIS"
          }
          
          # Add NSIS to PATH for current session and future steps
          if (Test-Path $nsisPath) {
            $env:PATH = "$nsisPath;$env:PATH"
            echo "$nsisPath" >> $env:GITHUB_PATH
            Write-Host "NSIS installed at: $nsisPath"
            Write-Host "Added to PATH: $nsisPath"
          } else {
            Write-Error "NSIS installation path not found: $nsisPath"
            exit 1
          }
          
          # Verify NSIS installation
          Write-Host "Verifying NSIS installation..."
          & "$nsisPath\makensis.exe" /VERSION
          if ($LASTEXITCODE -ne 0) {
            Write-Error "NSIS verification failed"
            exit 1
          }
          Write-Host "NSIS verified successfully"
      
      - name: Extract packages
        run: |
          $version = "${{ steps.version.outputs.version }}"
          
          # Extract with-python package
          if (Test-Path "dist/windows/lens-package-with-python-$version.zip") {
            Expand-Archive -Path "dist/windows/lens-package-with-python-$version.zip" -DestinationPath "build/windows" -Force
          } elseif (Test-Path "lens-package-with-python-$version.zip") {
            Expand-Archive -Path "lens-package-with-python-$version.zip" -DestinationPath "build/windows" -Force
          }
          
          # Extract requires-python package
          if (Test-Path "dist/windows/lens-package-requires-python-$version.zip") {
            Expand-Archive -Path "dist/windows/lens-package-requires-python-$version.zip" -DestinationPath "build/windows" -Force
          } elseif (Test-Path "lens-package-requires-python-$version.zip") {
            Expand-Archive -Path "lens-package-requires-python-$version.zip" -DestinationPath "build/windows" -Force
          }
      
      - name: Prepare NSIS build
        run: |
          $version = "${{ steps.version.outputs.version }}"
          Write-Host "Building installers with version: $version"
          # Version is passed via command line, no need to modify NSIS files
      
      - name: Compile installer (with Python)
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $buildDir = "${{ github.workspace }}\build\windows"
          $outDir = "${{ github.workspace }}\dist\windows"
          New-Item -ItemType Directory -Force -Path $outDir | Out-Null
          makensis /DVERSION="$version" /DBUILD_DIR="$buildDir" /DOUTDIR="$outDir" "${{ github.workspace }}\scripts\windows\installer-with-python.nsi"
          if (Test-Path "$outDir\lens-setup-with-python.exe") {
            Move-Item "$outDir\lens-setup-with-python.exe" "$outDir\lens-setup-with-python-$version.exe" -Force
          }
      
      - name: Compile installer (requires Python)
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $buildDir = "${{ github.workspace }}\build\windows"
          $outDir = "${{ github.workspace }}\dist\windows"
          New-Item -ItemType Directory -Force -Path $outDir | Out-Null
          makensis /DVERSION="$version" /DBUILD_DIR="$buildDir" /DOUTDIR="$outDir" "${{ github.workspace }}\scripts\windows\installer-requires-python.nsi"
          if (Test-Path "$outDir\lens-setup-requires-python.exe") {
            Move-Item "$outDir\lens-setup-requires-python.exe" "$outDir\lens-setup-requires-python-$version.exe" -Force
          }
      
      - name: Upload installers as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-installers
          path: |
            dist/windows/lens-setup-*.exe
          retention-days: 30
      
      - name: Upload to release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/windows/lens-setup-*.exe
          tag: v${{ steps.version.outputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
